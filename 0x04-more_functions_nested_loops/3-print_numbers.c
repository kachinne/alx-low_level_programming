#include "main.h"
/**
 * print_numbers - printing of numbers
 *
 * Return: Always 0
 */
void print_numbers(void)
{
	int num = '9';

	for (num >= '0' && num <= '9')
		_putchar('num');
	_putchar('\n');
	return (0);
}
