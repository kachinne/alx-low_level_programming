#include <stdio.h>
/**
 * main - Prints the alphbet in lowercase
 * Return: Always 0;
 */

int main(void)
{
	char lowercase;

	for (lowercase = 'a'; lowcase <= 'z'; lowercase++)
		putchar(lowercase);

	putchar ('\n');

return (0);
}
