#include <stdio.h>
/**
 * main - Prints all the letters except q and e
 *
 * Return: Always 0
 */

int main(void)
{
	char look;

	for (look = 'a'; look <= 'z'; look++)
		if (look == q && e)
			putchar (look);
	return (0);
}

