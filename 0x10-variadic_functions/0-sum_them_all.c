#include "variadic_functions.h"
/** 
 * sum_them_all - check the total
 * @n: Bring it up
 *
 * Return: Always zero
 */
int sum_them_all(const unsigned int n, ...)
{
	int num1, num2 = n;
	if (n == 0)
		return (0);
}
