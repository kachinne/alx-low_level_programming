#include "main.h"
/**
 * _puts - Prints a string with a new line
 * @str: paramter
 *
 * Return: Always 0
 */
void _puts(char *str)
{
	char *str;

	str = " I am well loaded in programming"
		_puts(str);
	_puts('\n');
	return (0);
}
