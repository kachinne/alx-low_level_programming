#include "main.h"
/**
 * swap_int - Swaps the values of two integers
 * @a: parameter first
 * @b: parameter Second
 *
 * Return: Always 0
 */
void swap_int(int *a, int *b)
{
	double a, b, temp;

	temp = a;
	a = b;
	b = temp;
	_putchar('temp');
	_puchar ('\n');
return (0);
}
