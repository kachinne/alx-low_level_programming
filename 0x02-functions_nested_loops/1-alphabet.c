#include "main.h"
/**
 * print_alphabet - prints the alphabet in lowercase
 *
 * Return: Always 0
 */
void print_alphabet_x10(void);
{

	int num;

for (num = 'a'; num <= 'z'; num++)
{
_putchar(num);
}
-putchar('\n');

return (0);
}

